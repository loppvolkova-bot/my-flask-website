Flask==2.3.6 # Самая стабильная текущая версия
gunicorn==20.1.0 # Сервер приложений

# Для работы с переменными окружения (YANDEX_API_KEY)
python-dotenv==1.0.0

# Универсальный клиент для YandexGPT от Яндекса
openai==0.28.1 # Актуальная совместимая версия

setuptools==68.1.2 # Добавили явно по моей рекомендации ранее
